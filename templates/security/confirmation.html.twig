{% extends 'base.html.twig' %}

{% block body %}

    {% if user %}

        <h5>Congratulations!</h5>

        <p>Your account is successfully confirmed!</p>

        For Sign in please follow this
        <a href="{{ path('security_login') }}">link</a>
        {% else %}
        <p>Sorry, something went wrong! Please check your email.</p>
    {% endif %}
{% endblock %}