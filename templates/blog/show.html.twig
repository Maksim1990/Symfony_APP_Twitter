{% extends 'base.html.twig' %}
{% block title %}{{ post.title }}{% endblock %}
{% block body %}
    <div>
        <a href="{{ path('blog_index') }}">Home</a>
    </div>
    <p><b>Title:</b>{{ post.title }} </p>
    <p><b>Id:</b>{{ id }}</p>
    <p><b>Text:</b>{{ post.text }}</p>
    <p>{{ 400|price }}</p>
    <p>{{ 400|add_salt }}</p>
    <p>{{ message_twig }}</p>
    <p>{{ test_twig_message }}</p>
{% endblock %}
